// main.jsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css"; // or App.css

ReactDOM.createRoot(document.getElementById("root")).render(
  // You can wrap in <React.StrictMode> if you like
  <App />
);
